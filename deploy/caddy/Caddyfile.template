{
  servers {
    trusted_proxies static private_ranges
  }
}

__PM_AGENT_SITE_ADDRESS__ {
  encode zstd gzip

  @api path /api*
  reverse_proxy @api api:8000 {
    flush_interval -1
  }

  reverse_proxy web:3000
}
