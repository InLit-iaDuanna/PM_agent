# Prompt Registry

Prompt templates are versioned here for the planner, research workers, verifier, synthesizer, and dialogue agent.

The runtime currently uses deterministic scaffolding logic, but the prompt contracts are ready for `MiniMax-M2.7` integration.

